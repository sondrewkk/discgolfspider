Run a crawler and output the results to a log file:
    rm scrapy.log && clear && scrapy crawl discshopen --logfile scrapy.log 

Run crawling interactivily in the shell:
    scrapy shell https://site.com